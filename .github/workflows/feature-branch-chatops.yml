---
name: feature-branch-chatops
on:
  issue_comment:
    types: [created]

permissions:
  pull-requests: write
  id-token: write
  contents: write

jobs:
  terraform-module:
    uses: nhanlh97/Nhanlh97-Mork-2023/.github/workflows/feature-branch-chatops.yml@main
    secrets:
      github_access_token: ${{ secrets.REPO_ACCESS_TOKEN }}
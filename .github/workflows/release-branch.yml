---
name: release-branch
on:
  push:
    branches:
      - main
      - release/**
    paths-ignore:
      - '.github/**'
      - 'src/**'
      - 'test/**'

permissions:
  contents: write
  id-token: write

jobs:
  terraform-module:
    uses: /Users/nhanlh97/IdeaProjects/spring-thymeleaf/.github/workflows/release-branch.yml@main
    secrets:
      github_access_token: ${{ secrets.REPO_ACCESS_TOKEN }}